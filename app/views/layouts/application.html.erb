<!DOCTYPE html>
<html>
<head>
  <title>StackOverflow</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
      <% if current_user %>
        <%= link_to current_user.email, user_path(current_user), class: ' pull-left'%>
        <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: ' pull-right'%>
      <% else %>
        <%= link_to 'Sign In or Sign Up', new_user_session_path,  class: ' pull-right'%>
      <% end %>
  </div>
</nav>
<div class="container">
  <%=flash[:notice]%>
  <%=flash[:alert]%>
  <%= yield %>
</div>
</body>
</html>
